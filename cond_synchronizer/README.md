## Synchronize leader/follower threads with conditional variables.

#### To run,
    $ make run

    The result should have not race conditions:
        Leader has walked.
        Follower has walked.